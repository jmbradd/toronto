/**
 * Created by jmbradd on 12/12/2015.
 */
app.controller("chatController", ['$scope', 'quizService', 'socketService', 'userService', function($scope, quizService, socketService, userService) {

    console.log("hey the fucking chat controller loaded")
    //$scope.qs = quizService
   // $scope.us = userService
   // $scope.room = $scope.qs.room







}]);